# Prometheus scrape example (optional)
# Point Prometheus at Nginx /metrics or the api service on the internal network.

scrape_configs:
  - job_name: railvoice-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
        labels:
          service: railvoice
